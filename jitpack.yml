jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.10-open
  - sdk use java 17.0.10-open

install:
  - ./gradlew :fastlink-speedtest-core:publishToMavenLocal :fastlink-speedtest-ui:publishToMavenLocal -x test
